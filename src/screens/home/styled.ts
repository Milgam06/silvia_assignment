import { StyleSheet } from "react-native";

export const HomeStyle = StyleSheet.create({
  titleView: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  buttonContianer: {
    flex: 1,
    alignItems: "center",
    gap: 20,
    marginTop: 80,
  },
});

import { StyleSheet } from "react-native";

export const TextStyle = StyleSheet.create({
  default: {
    fontSize: 12,
    fontWeight: 300,
    color: "#000",
  },

  test: {
    fontSize: 48,
    fontWeight: "semi-bold",
    color: "#000",
  },
});

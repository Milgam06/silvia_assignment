import { StyleSheet } from "react-native";

export const TextStyle = StyleSheet.create({
  default: {
    fontSize: 12,
    fontWeight: "300",
    color: "#000",
  },

  questionBoxKey: {
    fontSize: 72,
    fontWeight: "bold",
    color: "#000",
  },

  testKey: {
    fontSize: 36,
    fontWeight: "300",
    color: "#000",
  },
});
